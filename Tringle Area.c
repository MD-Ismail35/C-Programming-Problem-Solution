#include<stdio.h>
int main()
{
    float base,height,area;
    printf("Enter Base= and Height=");
    scanf("%f %f",&base,&height);
    area =(float)1/2 * base * height;
    printf("Area = %f",area);
    return 0;

}
